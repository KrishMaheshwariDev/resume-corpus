# General Resume Variants

The repository-root `resume.tex` is the active canonical general Java/backend resume.

Use this directory only if maintaining additional general variants such as:
- Java backend 1-page;
- Java/backend 2-page;
- cloud-backend general;
- full-stack general.

Do not create extra variants without a clear use case.
